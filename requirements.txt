Django==6.0.7
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
adrf
drf-spectacular==0.30.0
django-cors-headers==4.9.0
django-ratelimit==4.1.0
python-decouple==3.8
qrcode==8.0
Pillow==11.1.0
openpyxl==3.1.5
# Clasificador local de tickets (categoría, ver tickets/ml/) — TF-IDF +
# regresión logística entrenados con el dataset semilla, sin llamar a
# ninguna API externa.
scikit-learn==1.7.2
pandas==2.3.3
joblib==1.5.3
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==7.1.0
PyMySQL==1.1.1
# Requerido por PyMySQL para autenticar contra MySQL 8 (plugin por defecto
# caching_sha2_password) — sin esto, cualquier conexión a GLPI revienta con
# RuntimeError antes de negociar la auth, aunque la conexión TCP funcione.
cryptography==43.0.3
psycopg[binary]==3.2.3
pybreaker==1.2.0
httpx==0.28.1
redis==5.2.1
# Verifica la firma del id_token que devuelve Google en el login OAuth
# (bff/auth_views.py) contra las llaves públicas de Google — sin esto
# tocaría decodificar el JWT a mano sin validar quién lo firmó.
google-auth==2.36.0
# Transporte HTTP que exige google.auth.transport.requests.Request()
# para esa verificación (google-auth no trae uno propio con httpx).
requests==2.34.2
# Login LDAP institucional (junto a Google OAuth, ver config/settings.py).
# Necesita las libs de sistema libldap2-dev/libsasl2-dev en la imagen
# Docker para compilar python-ldap — ver Dockerfile.
django-auth-ldap==5.1.0

